//Map 

//Produces a new array of values by mapping each value in list trhough a transformation function (iterator).
//Each invocation of iterator is called with three arbuments: (element, indes, list). if "list" is a JavaScript object, itterator's aruments will be (value,key,list)
